// Deprecated. Replaced by the dedicated /pocket/[id] detail page which
// provides a richer view (snapshot stats, big-price live tracker per
// underlying, full KO schedule with upcoming/past split). The inline-
// expand approach in this file is no longer used and is kept only to
// avoid the empty-file warning from some toolchains.
//
// Safe to delete on any workstation that allows it.

export {};
